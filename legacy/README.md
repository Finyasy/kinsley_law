# Legacy Archive

This folder contains the retired pre-cutover codebase:

- `react-frontend/`: the old Create React App frontend
- `rails-backend/`: the old Ruby on Rails API
- `start-app-react-rails.sh`: the archived startup script for that split stack
- `README-react-rails.md`: the archived root README from the split-stack phase

These files are preserved for reference only. The active application now lives
at the repository root.
